# Copy this file to .env and set your own values.
# .env is gitignored and must never be committed.

# Root password for the MySQL container, also used by the web service to connect.
MYSQL_ROOT_PASSWORD=change-me
